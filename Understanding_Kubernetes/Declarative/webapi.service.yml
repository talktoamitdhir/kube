apiVersion : v1
kind : Service
metadata : 
  name : my-web-api
  labels : 
    app : my-web-api
spec :  
  #type : NodePort
  type : LoadBalancer
  selector :
    app : my-web-api
  ports :
    - name : http
      port : 80
      targetPort : 80
      #nodePort : 31000